# NIDS runtime dependencies.
#
# Intentionally minimal: ML models are implemented from scratch with NumPy,
# so we avoid sklearn / xgboost / torch in the production image. The rest
# supports the web API, config parsing, and testing.

numpy>=1.24
pyyaml>=6.0
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5

# Dev / test
pytest>=7.4
pytest-asyncio>=0.23
httpx>=0.26          # fastapi TestClient uses httpx
